html
  overflow-x hidden
  font-family mono-space, _paragraph, helvetica, sans-serif
  font-size _base
  color #333
  +wide()
    font-size _wide
  +desktop()
    font-size _desktop
  +tablet()
    font-size _tablet
  +mobile-landscape()
    font-size _mobile
  +mobile()
    font-size _mobile